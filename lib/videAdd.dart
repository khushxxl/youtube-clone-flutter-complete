import 'package:flutter/material.dart';

class VideoAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is Video Add Tab"),
      ),
    );
  }
}
