import 'package:flutter/material.dart';

class SubscribedScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is Subscription Tabs"),
      ),
    );
  }
}
